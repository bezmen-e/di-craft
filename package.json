{
	"name": "di-craft-monorepo",
	"private": true,
	"type": "module",
	"packageManager": "bun@1.4.2",
	"workspaces": [
		"packages/*",
		"apps/*"
	],
	"scripts": {
		"bench": "mise run bench",
		"build": "mise run build",
		"check": "mise run check",
		"checks": "mise run checks",
		"ci": "mise run ci",
		"fix": "mise run fix",
		"format": "mise run format",
		"lint": "mise run lint",
		"lint:fallow": "mise run lint:fallow",
		"lint:fallow:fix": "mise run lint:fallow:fix",
		"lint:fallow:fix:preview": "mise run lint:fallow:fix:preview",
		"lint:fix": "mise run lint:fix",
		"prepare": "mise run --no-deps prepare",
		"profile": "mise run profile",
		"test": "mise run test",
		"typecheck": "mise run typecheck",
		"typecheck:examples": "mise run typecheck:examples",
		"validate": "mise run validate"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.12",
		"fallow": "3.22.0"
	}
}
